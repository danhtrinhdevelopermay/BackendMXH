services:
  - type: web
    name: shatter-web
    env: node
    plan: free
    rootDir: web
    buildCommand: npm install && npx expo export --platform web
    startCommand: npx serve dist -l $PORT
    envVars:
      - key: NODE_ENV
        value: production
